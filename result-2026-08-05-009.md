# instruction-2026-08-05-009 実行結果

- 実行日時: 2026-08-05
- 状態: IN_PROGRESS（push成功後にCOMPLETEDへ更新）
- 担当: Codex（Agent B: 実装・テスト / Agent A: 独立審査）
- 対象指示書: instructions/instruction-2026-08-05-009.md

## 実施内容

- root `AGENTS.md`を追加し、`.agents/AGENTS.md`と最新指示書への導線、審査・commit・push規則を明記。
- READMEを現行CLI・Manual job・Human Gate・実インタビュー運用・合成デモ・制限事項に合わせて全面更新。
- `doctor` / `doctor --json`、`next` / `next --json`を追加。
- project/interviewの`real`、`synthetic`、`unknown`データ区分と、レポート・status・doctor・nextでの警告表示を追加。
- 実パイロット用テンプレートと合成デモ手順を追加。
- versionを0.2.0へ更新し、CHANGELOGを追加。wheelへFixture、テンプレート、合成デモを同梱。

## doctor実装と診断項目

`project.yaml`、sources実体・外部パス、Persona/alternatives/Interviewスキーマ、引用と行番号、Manual job台帳（構文・request/response・waiting/failed・重複job_id・未知target）、Learn状態、AI補完参照、stale report、データ区分を検査する。JSONは`status/errors/warnings/checks/next_actions`を含む。

## next実装と状態判定

doctor errorを最優先し、idea/source未登録、工程未開始、Manual waiting jobのjob_id・target、Guide、Human Gate、stale report、完了後の未確認事項を最大3件提示する。インタビュー0件では実顧客インタビューを案内し、検証完了を提示しない。

## データ区分

既存プロジェクトはunknownとして後方互換に扱う。Mockのみのプロジェクトはsynthetic、`add-interview --data-classification real`はrealとして保存し、synthetic/unknownのレポートには実顧客検証・CPF確立・市場成立を示さない警告を出す。

## README実行確認

一時プロジェクトで`--help`、`init`、Mock Draw、`doctor --json`、`next --json`、`report`を実行し、synthetic区分と非検証警告を直接確認した。READMEのManual手順、同意・匿名化・録音許可・具体的行動の説明をコードとCLIに照合した。

## テンプレート一覧

- `templates/real-pilot/idea-template.md`
- `templates/real-pilot/source-metadata-template.yaml`
- `templates/real-pilot/interview-consent-template.md`
- `templates/real-pilot/interview-guide-notes.md`
- `templates/real-pilot/interview-record-template.md`
- `templates/real-pilot/manual-response-examples/README.md`
- `examples/synthetic-demo/README.md`

## wheel検証

`pip wheel --no-deps`でwheelをビルドし、ZIP内の`needs_detector/fixtures/llm/`、`templates/real-pilot/`、`examples/synthetic-demo/`を直接検査。インストール先だけをsys.pathに設定し、help、init、doctor、next、Mock Drawを実行した。

## 実行したテストと結果

- `py -m pytest -o pythonpath=src tests/ -q`: 35 passed
- `py -m pytest -o pythonpath=src tests/ --cov=src/needs_detector -q`: 35 passed、TOTAL 65%
- `py -m pytest -o pythonpath=src tests/integration/test_wheel_packaging.py -v`: passed
- 警告: pytest_freezegun由来の外部DeprecationWarning 70件

## 審査サイクル

- Cycle 1: REVISE（wheelのroot資材同梱不足、synthetic分類のレポート反映不足）
- Cycle 2: PASS（コード、CLI出力、JSON、生成物、wheelを直接確認）
- Cycle 3/4: 未実施（Cycle 2でPASS）

## 未解決事項

なし。`dream.md`は今回の指示どおり変更していない。既存の未追跡ファイルは削除・変更・stashせず保持している。

- Gitブランチ: main
- commit: TO_BE_FILLED
- push結果: TO_BE_FILLED
