# Needs Detector

CLI application for continuous needs validation using LLM.

## Installation

```bash
pip install -e .[dev]
```

## Usage

```bash
needs-detector init my-project
needs-detector add-idea "Make it easier to organize memos"
# ...
```
