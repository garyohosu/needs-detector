# instruction-2026-08-05-010 実行結果

- 実行日時: 2026-08-05
- 状態: COMPLETED
- 担当: Codex（Agent B: 実装・テスト / Agent A: 独立審査）
- 対象指示書: instructions/instruction-2026-08-05-010.md

## 外部監査で発見された不具合

- source indexの`file_name`をproject直下で解決し、正常add-sourceをmissing扱いしていた。
- index.yamlだけでもsource登録済みとnextが誤判定していた。
- reportが存在しない`interview_<target>.md`を引用元として表示していた。
- realを1件含むsynthetic/unknown混在をrealに集約する可能性があった。

## 実施内容

- `load_source_entries()`を追加し、add-source、Draw、Doctor、Nextで`project/sources/<file_name>`を共有。絶対・UNC・Windowsドライブ・`..`・外部参照・型不正・重複を拒否。
- add-interviewで入力バイトを`interviews/raw/<id>.md`へそのまま保存し、derived YAMLに`source_file`とSHA-256を追加。重複登録を拒否。
- 引用検証・report表示・doctor診断をraw実体の行へ統一。raw改変・欠落・hash不一致をerror、legacy YAMLはwarningで非破壊扱い。
- real/synthetic/unknownの個別区分と集約区分real/synthetic/unknown/mixedを実装し、status・doctor・next・reportで分類と内訳を統一。
- READMEとCHANGELOGをraw構造、引用パス、mixed分類、legacy扱いへ更新。

## source診断結果

正常なadd-source後に実CLI `doctor --json`を実行し、終了コード0かつsource check=okを確認。indexだけの状態ではnextがadd-sourceを提示。missing、絶対、UNC、Windows絶対、`../`、malformed index、重複をdoctor errorとして確認。

## next状態遷移試験

init直後、ideaのみ、sourceのみ、idea+source、Draw/Explore/Guide完了、interview 0件、Manual waiting、Learn完了・reportなし、stale report、全工程完了を既存・追加テストで確認。doctor errorは常に最優先で、source index自体を資料として数えない。

## rawインタビュー・引用検証

rawファイル存在、入力バイトSHA-256一致、reportの`[interviews/raw/<id>.md:L2]`実在、指定行のquote一致を直接確認。raw変更・削除、重複登録は検出。legacy YAMLはsource_fileなしwarningで読み込みを継続。

## データ区分試験

realのみ、syntheticのみ、unknownのみ、real+synthetic、real+unknown、synthetic+unknown、interview 0件でproject.yamlがsyntheticのケースを確認。混在はmixedとなり、実顧客検証済み・CPF確立・市場成立とは表示しない。

## legacy互換確認

旧形式の`source_file`なしinterview YAMLを削除・変換せず、content/refutationsを読み、doctor warningとして扱うことを確認。

## wheel検証

wheelをビルドし、Fixture・templates・examplesをZIP検査。インストール先のみを参照してinit、add-source、add-interview --data-classification real、doctor、next、report、Mock Fixture読込を実行。

## 実行したテストと結果

- `py -m pytest -o pythonpath=src tests/ -v`: 41 passed
- `py -m pytest -o pythonpath=src tests/ --cov=src/needs_detector`: 41 passed、TOTAL 71%
- `py -m pytest -o pythonpath=src tests/integration/test_wheel_packaging.py -v`: passed
- 警告: pytest_freezegun由来の外部DeprecationWarning 82件

## 審査サイクル

- Cycle 1: REVISE（raw改行変換によるSHA-256不一致、wheel CLI試験の文字列エスケープ不備）
- Cycle 2: PASS（コード、CLI、JSON、raw、report、分類、wheelを直接確認）
- Cycle 3/4: 未実施（Cycle 2でPASS）

## 作成・変更ファイル

- `DESIGN.md`, `TESTPLAN.md`, `README.md`, `CHANGELOG.md`
- `src/needs_detector/core/services.py`
- `tests/integration/test_instruction_010.py`, `tests/integration/test_wheel_packaging.py`
- `records/agent-a-review-010-cycle-1.md`, `records/agent-a-review-010-cycle-2.md`
- `records/agent-b-implementation.md`, `result-2026-08-05-010.md`

## 未解決事項

なし。`result-2026-08-05-009.md`は改変していない。既存の未追跡ファイルは削除・変更・stashしていない。`dream.md`も変更していない。

- Gitブランチ: main
- commit: e0a81a4（010実装・審査・結果記録）
- push結果: SUCCESS（origin/mainへpush済み）
