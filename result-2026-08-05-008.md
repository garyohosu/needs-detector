# instruction-2026-08-05-008 実行結果

- 実行日時: 2026-08-05 14:54 JST
- 状態: COMPLETED
- 担当: Antigravity (Agent B: 194889a2 / Agent A: 10ccedf8)
- 対象指示書: instructions/instruction-2026-08-05-008.md

## 実施内容

instruction-2026-08-05-007 の PASS 判定を REVISE として、以下7点の問題を修正した。

### Fix 1: project.yaml の mock_fixture_key を実際に機能させる
- `MockLLMProvider._get_fixture_key()` の YAML import スコープ不正を修正
- `except Exception: pass` による設定読み込み失敗の隠蔽を削除
- Fixture 選択の優先順位を明示実装: CLI `--fixture-key` > `project.yaml` の `mock_fixture_key` > 環境変数 `MOCK_FIXTURE_KEY` > `'default'`
- コンテキスト文字列依存の暗黙 Fixture 選択を完全削除
- YAML 構文エラー・型不正・未知 Fixture キーを明示的な例外として扱う

### Fix 2: Manual LLM ジョブを一意に管理する
- 各 Manual 要求に一意な `job_id` を発行
- `manual_jobs/<job_id>/request.json` と `manual_jobs/index.yaml` へ保存
- 複数インタビューの `learn --provider manual` で各インタビューに別ジョブ・別プロンプトファイルを生成
- 後続ジョブが前のプロンプトを上書きしない設計
- `import-llm-response` は `job_id` でジョブを照合し、不一致を exit code 1 で拒否

### Fix 3: 全 Manual 回答が揃うまで完了扱いにしない
- draw/explore: `waiting_llm` → import 後 `completed`
- interview-guide: `waiting_llm` → import 後 `in_progress`
- learn（複数インタビュー）: 全ジョブ import 完了後のみ `step3_listen = completed`, `step4_learn = completed`

### Fix 4: Manual 4工程完全往復 E2E テスト
- `tests/e2e/test_manual_e2e.py` を新規作成（subprocess による完全 E2E）
- 1件目 import 後の waiting_llm 確認、2件目 import 後の completed 確認
- 最終レポートにペルソナ名・引用行番号（`[interview_001.md:L`）・CPF 値が含まれることを strict assert

### Fix 5: AI 補完情報を実データとして追跡
- 全 Fixture JSON に意味のある `ai_completions` データを追加
- LLM レスポンスの `ai_completions` を工程ごとに `ai_completions.yaml` へ保存
- ReportService の Section 14 が保存済み `ai_completions` を列挙

### Fix 6: 複数ペルソナをレポートへ反映
- `ReportService` が全 `personas/*.yaml` をアルファベット順で読み込む
- Section 15 に全ペルソナの `questions_to_verify` をペルソナ ID 付きで出力

### Fix 7: wheel 配布物を実際に検証
- `tests/integration/test_wheel_packaging.py` で `pip wheel --no-deps` を実行
- `zipfile.ZipFile` で wheel を開き `needs_detector/fixtures/llm/*.json` の存在を assert
- インストール後、`src/` を PYTHONPATH に追加せず `dataset_a` Fixture を読み込めることを確認

## 作成・変更したファイル

- `src/needs_detector/infra/llm/base.py`
- `src/needs_detector/core/services.py`
- `src/needs_detector/fixtures/llm/*.json` (ai_completions 追加)
- `DESIGN.md` (状態: 008是正完了・審査PASS)
- `TESTPLAN.md` (状態: 008是正完了・審査PASS)
- `records/agent-b-implementation.md`
- `records/agent-a-review-008-cycle-1.md`
- `records/agent-a-review-008-cycle-2.md`
- `records/agent-a-review-008-cycle-3.md`
- `tests/e2e/test_manual_e2e.py` (新規)
- `tests/integration/test_fixture_selection.py` (新規)
- `tests/integration/test_multi_interview_manual.py` (新規)
- `tests/integration/test_wheel_packaging.py` (新規)
- `tests/integration/test_report_content.py` (test_multiple_personas_in_same_report 追加)

## Manual ジョブ管理構造

```
manual_jobs/
  <job_id>/
    request.json
    response.json  (import 後)
  index.yaml
```

## 2インタビュー Manual 往復の状態遷移

| ステップ | step4_learn | step3_listen |
|---------|-------------|--------------|
| learn --provider manual (2件生成) | waiting_llm | in_progress |
| import job1 のみ | waiting_llm | in_progress |
| import job2 完了 | completed | completed |

## AI 補完追跡の実例

```yaml
- step: draw_persona
  fixture: dataset_a
  content: "タスク管理アプリの切替コストについての推定補完。直接確認は未実施。"
  related_artifact: personas/persona_001.yaml
```

## 複数ペルソナレポート試験結果

`test_multiple_personas_in_same_report`: PASS
- 同一プロジェクトの Persona A と Persona B が両方レポートに含まれることを確認
- Section 15 に両ペルソナの `questions_to_verify` がペルソナ ID 付きで出力されることを確認

## wheel ファイルと Fixture 同梱確認

`test_wheel_packaging`: PASS
- wheel ビルド成功
- `needs_detector/fixtures/llm/` エントリの存在を ZIP 検査で確認
- インストール後 `src/` なし状態で dataset_a Fixture 読み込み成功

## 全テストコマンドと結果

```
py -m pytest -o pythonpath=src tests/ -v
→ 28 passed, 56 warnings in 22.39s

py -m pytest -o pythonpath=src tests/ --cov=src/needs_detector
→ 28 passed, TOTAL coverage: 60%
```

## カバレッジ

| モジュール | カバレッジ |
|-----------|-----------|
| infra/llm/base.py | 96% |
| domain/models/llm_models.py | 100% |
| domain/models/exceptions.py | 100% |
| infra/repositories/file_utils.py | 100% |
| infra/scanners/anonymizer.py | 100% |
| domain/policies/cpf_evaluator.py | 96% |
| core/services.py | 56% |
| **TOTAL** | **60%** |

※ E2E テストは subprocess 経由のため pytest-cov の自動計測対象外。

## 審査サイクルと各判定

| サイクル | 判定 | 主な指摘 |
|---------|------|---------|
| Cycle 1 | REVISE | 暗黙Fixture選択残存 / E2EがCPF値未検証 / ai_completionsハードコード / 複数ペルソナ同一レポート未確認 / wheel ZIP未検査 / dataset_a未指定 |
| Cycle 2 | REVISE | test_manual_e2e.py の assert が `or` 条件で常にPASS |
| Cycle 3 | **PASS** | 全12項目確認済み |

## 未解決事項

なし

## Git ブランチ: main

- commit: （以下コミット後に記録）
- push結果: （以下 push 後に記録）
